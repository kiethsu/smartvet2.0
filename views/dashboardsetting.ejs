<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Dashboard Settings</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-4">
    <h1>Dashboard Settings</h1>
    <form id="dashboardSettingsForm" action="/settings/update-dashboardsetting" method="POST">
      <!-- Quick Guide Field -->
      <div class="form-group">
        <label for="quickGuide">Quick Guide</label>
        <textarea id="quickGuide" name="quickGuide" rows="3" class="form-control"><%= dashboardSetting && dashboardSetting.quickGuide ? dashboardSetting.quickGuide : "" %></textarea>
      </div>
      <!-- Welcome Text Field -->
      <div class="form-group">
        <label for="welcomeText">Welcome Text</label>
        <textarea id="welcomeText" name="welcomeText" rows="3" class="form-control"><%= dashboardSetting && dashboardSetting.welcomeText ? dashboardSetting.welcomeText : "" %></textarea>
      </div>
      <!-- Tutorial Video URL Field -->
      <div class="form-group">
        <label for="videoUrl">Tutorial Video URL (YouTube embed URL or Google Drive share link)</label>
        <input type="text" id="videoUrl" name="videoUrl" class="form-control" value="<%= dashboardSetting && dashboardSetting.videoUrl ? dashboardSetting.videoUrl : "" %>">
      </div>
      <button type="submit" class="btn btn-primary">Save Settings</button>
    </form>
  </div>
</body>
</html>
